# === SERVER CONFIGURATION ===
server.port=${PORT:8080}
server.servlet.context-path=/

# === POSTGRESQL DATABASE CONFIGURATION ===
# TRANSACTION POOLER for optimal performance (Port 6543)
spring.datasource.url=jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true&prepared_statements=false&prepareThreshold=0
spring.datasource.username=postgres.ieesukcrbwebduaqupzv
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# === ALTERNATIVE: DIRECT CONNECTION (comment above, uncomment below if needed) ===
# Direct connection to Supabase (Port 5432) - slower but simpler
#spring.datasource.url=jdbc:postgresql://db.ieesukcrbwebduaqupzv.supabase.co:5432/postgres
#spring.datasource.username=postgres
#spring.datasource.password=${DATABASE_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA/HIBERNATE CONFIGURATION ===
# Optimized for Transaction Pooler
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# === CONNECTION POOLING & PERFORMANCE ===
# Optimized for Transaction Pooler
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.idle-timeout=180000
spring.datasource.hikari.leak-detection-threshold=60000

# === APPLICATION CONFIGURATION ===
spring.application.name=TodoAPI
logging.level.com.pavel.todoapi=DEBUG
logging.level.org.springframework.web=INFO
